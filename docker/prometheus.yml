# Minimal Prometheus config scraping the BYO-SLM API /metrics endpoint.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: byo-slm
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
